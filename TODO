//  ---------------------------------------------- TODO
TODO: InverseRotation function. 

TODO: renderWindow class need to be chieldren of sf::RenderWindow to use the virtual onResize() function.

TODO: void MainMenu(&window) then settingMenu(&window) etc...

TODO: make an array of the rects to change the frame, could make a vector but its too heavy for just some frames.~

TODO: make a font.

TODO: Tetromino class has a lot of public fuction who could be private.

TODO: Put a Icon on the window, and to the .exe aswell.

TODO: Clean the code.

//  ---------------------------------------------- NOTES

NOTE: Test rotation with 4 width, maybe with 3 width can't make t-spin/S-Spo etc

NOTE: See if can create a class named Tile to use.

NOTE: Use Rotation based on center of mino or center of grid? Check tetromino L and Tetromino J to see difereces.

NOTE: Tetromino I rotation is broken

TODO: Need to create a render class. --maybe. I made a simple class who can create a window, but for now i don't think it will be useful
TODO: Make a menu for the game, UI Class. Note: Made a simple fuction to buttons

//  ---------------------------------------------- COMPLISHED

+TODO: Need to clean the BoxCollision class, has a lot of parameter that I don't use.
+TODO: When press two keys, the piece ignore the cooldown need to fix.
+TODO: Game logic, make the pieces fall, and fix on the grid // Need to make the tetromino collide with the grid 
+TODO: Made the Update function who make handle the movement and make the tetrominos moves down, need to know how to know when the tetromino is on the floor and fix it to the ground adding to one Vector of minos -  also need to make the
spawn function who will be called after that, and at the beginning of the game.
+TODO: HardDrop is so fucking slow
+TODO: Rotation of Z is broken is flip before rotating, so when rotate it look like mino S. Kinda fixed it but now the 
first rotation ins't working on S and Z, just rotate at second time pressed, need to know how to fix it.
+TODO: Wall Kick n' Floor Kick
+TODO: Create a fuction to make the pontuation multiplier
+TODO: Need to implement rotation based on 4 to I Tetromino(Long) and 3 based index for the others.3
+TODO: make UI classes (Button, Texture(?Image(?)), Animation ...)
+TODO: make Wall kick again.
+TODO: see HardDrop fuction, it's not working sometimes, and so fucking slow
+TODO: use array [] of mino instead on Tetromino class.
+TODO: minos S/Z hard drop isn't working
+TODO: onClick(frame) onOver(frame) implement on Button Class
+TODO: See if can use array instead of vector on tetrominos class
+TODO: +NextTetromino stuff, +holdTetromino, +shadowTetromino.
